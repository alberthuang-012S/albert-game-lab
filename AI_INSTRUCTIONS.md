# 給 AI 的修改說明

你正在協助使用者把這份「個人 Game Lab 樣板」改成他自己的小遊戲入口網站。開始工作前，請先完整閱讀本文件與 `README.md`，再檢查現有檔案。

## 專案目標

這是一個個人小遊戲入口網站。網站會依分類顯示遊戲 Card；點擊 Card 後，在新分頁開啟已部署的遊戲網址。

此專案只是一個入口，不包含各款遊戲本體。

## 技術限制

只能使用：

- HTML
- CSS
- Vanilla JavaScript

不要加入：

- React、Vue、TypeScript
- npm、Node.js、package manager
- Bootstrap、Tailwind 或其他 framework
- bundler、build system
- 後端、API、資料庫
- 第三方 icon 或字型套件

不要工程化，也不要把目前五個檔案拆成更多程式檔案，除非使用者明確要求。

## 現有架構

- `index.html`：網站骨架、名稱與副標語。
- `style.css`：所有視覺樣式與響應式排版。
- `games.js`：分類和遊戲資料。
- `script.js`：依資料建立分類與 Card。
- `README.md`：給網站擁有者的維護說明。

## 首次客製化任務

請先向使用者取得以下資料；如果對方已經提供，就不要重問：

1. 網站名稱。
2. 首頁副標語。
3. 遊戲分類清單。
4. 每款遊戲的名稱、分類、簡介、網址。
5. 是否已有遊戲圖片；沒有就保留空字串。

拿到資料後：

1. 修改 `index.html` 的 `<title>`、`<h1>` 與副標語。
2. 刪除 `games.js` 中所有「範例遊戲」。
3. 依使用者資料更新 `categories` 與 `games`。
4. 不要自行加入使用者未提供的真實遊戲、品牌、公司名稱或作者資料。
5. 除非使用者要求改版，保留現有視覺風格與程式架構。

## 資料格式

分類：

```js
{
  id: "memory",
  name: "記憶類",
}
```

遊戲：

```js
{
  name: "遊戲名稱",
  category: "memory",
  description: "一句簡短的玩法介紹。",
  url: "https://example.com/",
  image: "",
}
```

規則：

- `category` 必須對應某一筆分類的 `id`。
- `url` 只能放純網址，不可放 Markdown 格式。
- 沒有圖片時使用 `image: ""`，不得放不存在的假圖片。
- 不要在 `script.js` 或 `index.html` 寫死遊戲資料。
- 不要加入重複遊戲。
- 沒有遊戲的分類可以留在資料中，畫面不會顯示空分類。

## 視覺與互動原則

- 低彩度、淺白藍、乾淨簡潔。
- Desktop 每排 3 張 Card、Tablet 2 張、Mobile 1 張。
- 整張 Card 可點擊，並以新分頁開啟遊戲。
- 無圖片時不可留下空白圖片框。
- 保持鍵盤操作、焦點樣式與合理的文字對比。
- 不可出現水平捲動。
- 不加入搜尋、篩選、登入、收藏、評分、dark mode 或複雜動畫，除非使用者明確要求。

## 修改邊界

如果使用者只要求新增或修改遊戲：

- 優先只改 `games.js`。
- 不要順手重構其他檔案。
- 不要改變現有功能、色彩或排版。

如果使用者要求修改名稱或副標語：

- 優先只改 `index.html`。

如果使用者要求調整視覺：

- 優先只改 `style.css`。
- 保留 HTML 結構與資料驅動方式。

只有當需求確實影響畫面生成邏輯時，才修改 `script.js`。

## 完成前檢查

請實際檢查：

1. 所有遊戲都出現在正確分類。
2. 每個 `category` 都有對應的分類 `id`。
3. 沒有保留「範例遊戲 A／B」。
4. 遊戲網址是純網址且能正確連結。
5. 沒有圖片時不顯示圖片區塊。
6. Desktop、Tablet、Mobile 排版合理。
7. 沒有水平捲動。
8. 沒有引入任何框架或第三方套件。
9. `README.md` 若因架構變更而失準，必須同步更新；若架構未變，不要無故重寫。

完成後，用簡短清單告訴使用者：

- 修改了哪些檔案。
- 每個修改的目的。
- 日後新增遊戲時，應該修改哪個檔案。

不要修改此專案資料夾以外的任何檔案。
